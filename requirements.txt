# OpenAI API - Chat completions y embeddings
# Usamos gpt-4o-mini y text-embedding-3-small
openai==2.3.0

# FastAPI - Framework web
fastapi==0.118.0
uvicorn==0.37.0          # Servidor ASGI para FastAPI
pydantic==2.11.10        # Validación de datos

# ChromaDB - Base de datos vectorial local
# Almacena embeddings y hace similarity search
chromadb==1.2.1

# NumPy - Requerido por ChromaDB para operaciones vectoriales
numpy==2.3.4

# Python-dotenv - Cargar variables de entorno desde .env
python-dotenv==1.1.1
requests==2.32.5
